// Configuration for IDEs only.
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Needed for Moment.js since it doesn't have a default export.
    "allowSyntheticDefaultImports": true,
    "rootDir": "..",
    "baseUrl": ".",
    "paths": {
      "@segment/carbon/*": ["../carbon/*"],
      "@segment/ui/*": ["../lib/*"],
      "@segment/ui": ["../lib/public-api.ts"]
    }
  },
  "include": ["./**/*.ts"]
}
